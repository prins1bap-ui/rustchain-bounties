name: RTC Auto-Pay on PR Merge

# Payout automation must only run in the canonical RustChain bounty repository.
# Forks inherit this workflow but do not own the canonical payout authority or
# payout secrets. Running it in a contribution fork creates guaranteed failures
# and, worse, would be unsafe if payout credentials were ever copied there.
on:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  auto-pay:
    if: github.repository == 'Scottcjn/rustchain-bounties'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Resolve merged PR from the pushed commit
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          pr_json=$(gh api "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
            -H "Accept: application/vnd.github+json" 2>/dev/null || echo '[]')
          number=$(echo "$pr_json" | jq -r '.[0].number // empty')
          author=$(echo "$pr_json" | jq -r '.[0].user.login // empty')
          echo "number=${number}" >> "$GITHUB_OUTPUT"
          echo "author=${author}" >> "$GITHUB_OUTPUT"
          if [ -z "$number" ]; then
            echo "No associated PR for this push — nothing to pay."
          else
            echo "Resolved PR #${number} by ${author}"
          fi

      - name: Set up Python
        if: steps.pr.outputs.number != ''
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.pr.outputs.number != ''
        run: pip install requests

      - name: Run RTC Auto-Pay
        if: steps.pr.outputs.number != ''
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ steps.pr.outputs.number }}
          REPO: ${{ github.repository }}
          PR_AUTHOR: ${{ steps.pr.outputs.author }}
          RTC_VPS_HOST: ${{ secrets.RTC_VPS_HOST }}
          RTC_ADMIN_KEY: ${{ secrets.RTC_ADMIN_KEY }}
          REPO_OWNER: ${{ github.repository_owner }}
        run: python scripts/auto-pay.py
